---
- name: Install and configure softether
  hosts: all
  gather_facts: true
  become: true
  collections:
    - name: i40sys.vpn_ansible_collection.softether
      vars:
        softether_vpnclient_path: "/docker-data/softether-vpnclient"
